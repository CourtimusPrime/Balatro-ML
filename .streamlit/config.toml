# Streamlit theme for the Balatro ML Training Monitor (DASH-03).
# Locked by 03-UI-SPEC.md § Color: dark base + Balatro Gold accent so native
# widgets (tab underline, metric deltas, focus rings) match the chart accent.
[theme]
base = "dark"
primaryColor = "#F0A500"
